<!-- Generated by ac:claude-md-rules-creator on YYYY-MM-DD. This comment is block-level HTML, stripped before injection: zero token cost for the model, visible when a human opens the file. -->

# CLAUDE.local.md

<!--
Personal project-specific preferences. Gitignored.

Before committing anything else, add this file to .gitignore:
echo "CLAUDE.local.md" >> .gitignore

Keep under 30 lines. Only include what makes Claude noticeably more useful for you.
-->

## My setup

- [Your role on this project, e.g., "I own src/billing/; flag changes outside that area."]
- [Sandbox URLs, e.g., "Dev: http://localhost:3000; staging: http://staging.example.test."]
- [Test account or credentials POINTER (not the credentials themselves), e.g., "Test account: see ~/.config/test-creds."]
- [Default branch, e.g., "I usually work in feature/oauth. Default there when I am vague."]

## Style

- [Personal communication preferences, e.g., "Be terse. Skip the end-of-turn summary."]
- [Per-project workflow preferences, e.g., "Surface a one-paragraph plan before touching src/billing/."]

## Tools

- [Personal tooling available, e.g., "I have Playwright MCP configured; use it for UI verification."]

<!--
If you use multiple worktrees of this repo (CLAUDE.local.md is gitignored, so it only exists in one tree), consider moving the content to ~/.claude/<project>-instructions.md and making this file a one-line stub:

@~/.claude/<project>-instructions.md

Then copy the stub into every worktree. Updates to the home-directory file propagate everywhere.
-->
