[//]: # (Seed template consumed by /ac:install when generating a user's my-coding skill.)
[//]: # (Placeholders in angle brackets are filled from the install interview.)
[//]: # (Do not edit this file to bake in personal content; it stays stack-agnostic.)

---
name: my-coding
description: <one-sentence skill description: enforce [developer name]'s coding style, conventions, and architectural philosophy when writing code. Core tenets: (1) <tenet>, (2) <tenet>, (3) <tenet>. Use for all code generation, implementation, refactoring, and review tasks.>
when_to_use: Any request to write, modify, review, or generate code in any language.
---

# <Developer Name> Coding Style

<1-2 sentences: what does "good code" mean to this developer?>

## North Star: <Inspiration Source>

<2-3 sentences: what existing codebase, framework, or style guide sets the gold standard? Apply these principles universally.>

- **<Principle 1>:** <concrete example>
- **<Principle 2>:** <concrete example>
- **<Principle 3>:** <concrete example>

## Step Zero: Detect Language Version

Detect the project's language version before writing the first line of code. Lock the feature set to what that version supports. If detection fails, ask the user.

| Language | Config file | Field | Success criterion |
|----------|-------------|-------|-------------------|
| <Language 1> | <config file> | <field> | <can name the version and ruled out features above it> |
| <Language 2> | <config file> | <field> | <can name the version and ruled out features above it> |

Add rows for each language in the project. Remove the table when the project is single-language with no version ambiguity.

## Non-Negotiable Rules

Each rule states the why. When the list does not name an edge case, fall back to the why.

### 1. Type every parameter and return value

Explicit type declarations on every parameter, return type, and property. The IDE, the static analyzer, and the next reader all benefit at zero ongoing cost. Untyped code does not ship.

```<language>
// CORRECT
function add(a: number, b: number): number {
    return a + b;
}

// WRONG - omits types, hides intent
function add(a, b) {
    return a + b;
}
```

### 2. English-only identifiers and comments

All identifiers, comments, docblocks, commit messages, and error messages are English. Tooling, search, and contributor reach all depend on a single language. Mixed-language codebases rot fast.

```<language>
// CORRECT
const userCount = getActiveUsers().length;

// WRONG - non-English identifier
const kullaniciSayisi = getActiveUsers().length;
```

### 3. Linter is law

Code passes the linter and static analyzer with zero warnings and zero errors. No suppressions (`@ts-ignore`, `# noqa`, linter-disable comments). Fix the actual issue. Every suppression is a deferred problem nobody comes back to.

<!-- Insert user-specific rules below this marker. Follow the format above: title + why + CORRECT/WRONG code block. Target 8-12 rules total. Suggested topics: documentation (docblocks on public APIs), line width, multi-line collections, trailing commas, import hygiene, TDD, step comments, enums for status values, thin controllers. -->
<!-- <USER_RULES_PLACEHOLDER> -->

## Architecture Principles

| Principle | Rule | Rationale |
|-----------|------|-----------|
| <Concept 1> | <one-sentence rule> | <why> |
| <Concept 2> | <one-sentence rule> | <why> |
| <Concept 3> | <one-sentence rule> | <why> |
| <Concept 4> | <one-sentence rule> | <why> |
| <Concept 5> | <one-sentence rule> | <why> |

Add 5-8 rows. Common concepts: service/repository separation, immutable value objects, event-driven side effects, constructor injection, guard clauses, thin controllers.

## Formatting Quick Reference

| Setting | Value |
|---------|-------|
| Line width | <max characters> |
| Indentation | <N spaces / tabs> |
| Trailing commas | <always / never / contextual> |
| Multi-line collections | <always / contextual> |
| Import order | <grouping rule: stdlib, third-party, local> |
| Linter | <tool name + zero-tolerance> |

## Testing Philosophy

<2-4 sentences: when are tests written (before or after code), which framework, what counts as sufficient coverage, and whether failing tests are required before a fix.>

Suggested minimum: failing test before every bug fix; new feature test before implementation; zero "we'll add tests later."

## Language-Specific References

Read the file matching the language before writing code. Do not pre-load files for languages not in play.

| Working in | Read |
|------------|------|
| <Primary language / framework> | `references/<language>.md` |
| Any language, before review or refactor | `references/anti-patterns.md` |

Add one row per primary language. Create the referenced file as part of the skill (target 200-400 lines per language file). Create `references/anti-patterns.md` as a 30-60 line "never do" list.
